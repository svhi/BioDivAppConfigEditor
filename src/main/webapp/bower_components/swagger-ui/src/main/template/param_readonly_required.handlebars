<td class='code required'>{{qName}}</td>
<td>
    {{#if isBody}}
        <textarea class='body-textarea'  readonly='readonly' placeholder='(required)' qName='{{qName}}'>{{defaultValue}}</textarea>
    {{else}}
        {{#if defaultValue}}
            {{defaultValue}}
        {{else}}
            (empty)
        {{/if}}
    {{/if}}
</td>
<td>{{{description}}}</td>
<td>{{{paramType}}}</td>
<td><span class="model-signature"></span></td>
